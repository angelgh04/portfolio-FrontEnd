export class NuevoUsuario {
    //Pongo el ! para que no tome en cuenta la inicialización de las variables
    nombre!: string;
    nombreUsuario!: string;
    email!: string;
    password!: string;
    authorities!: string[];
}
